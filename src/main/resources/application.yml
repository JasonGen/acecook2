# Local server
server:
  # port is used by spring-boot-admin
  port: 8080
  contextPath: /acecook

spring:
  datasource:
    password: admin
    url: jdbc:mysql://localhost:3306/bdacecook?user=root
  jpa:
    hibernate:
      show-sql: true